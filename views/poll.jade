extends layout

block content
  .container.text-center
    h1= poll.title
    br
    h3 Make your Selection
    br
    .jumbotron.align-center.text-left
      each val in poll.choices
        p= val
        input(type='button', name="#{val}-button" value='Select').btn

